## A demo of false start and session resume for BoringSSL

```bash
ln -s /path/to/boringssl/include .
ln -s /path/to/boringssl/build/crypto/libcrypto.a .
ln -s /path/to/boringssl/build/ssl/libssl.a .
make
```

